name: Build on PR
permissions:
  contents: read

on:
  pull_request:
    branches: [main, stable]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build-base.yml
    secrets:
      workflowToken: ${{ secrets.GITHUB_TOKEN }}
